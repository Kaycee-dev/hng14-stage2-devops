# FIXES

Canonical bug-fix ledger for Stage 2. All line numbers refer to the starter-repo
state observed on `2026-04-20` from the fork at `a98a2d1 first commit`.

Rules:
- one row per defect, with exact file path and exact line number or span
- describe the original defect, the concrete change, and how it was verified
- do not duplicate this ledger elsewhere

| ID    | File                | Line(s) | Problem | Change | Verification |
| ----- | ------------------- | ------- | ------- | ------ | ------------ |
| F-001 | `api/.env`          | 1-2     | Secret-shaped file (`REDIS_PASSWORD=supersecretpassword123`, `APP_ENV=production`) was committed under `git` tracking — direct violation of the task's "no `.env` in repo or history" rule. | Removed from tracked state via `git rm --cached api/.env`; deleted from working tree. Added `.gitignore` at repo root (lines 1-6) blocking `api/.env`, `worker/.env`, `frontend/.env`, and `.env*` except `.env.example`. Real values now flow only through a local, untracked root `.env` derived from `.env.example`. | `git status` shows `D api/.env`; `git ls-files api/.env` is empty; `git check-ignore api/.env` confirms ignore. **Follow-up:** the placeholder secret still exists in the starter commit in git history. Scrubbing requires a history rewrite (`git filter-repo`) which is a destructive, force-push operation and is pending explicit owner authorization before execution. |
| F-002 | `api/main.py`       | 8       | `r = redis.Redis(host="localhost", port=6379)` hardcoded Redis connection. Works on the developer laptop only; breaks inside containers and prevents password/db/host configuration. | Replaced with an env-driven client factory `_build_redis_client()` reading `REDIS_HOST`, `REDIS_PORT`, `REDIS_DB`, `REDIS_PASSWORD`; added `socket_connect_timeout=2` and `socket_timeout=2` so failures surface fast. Defaults to `redis:6379` to match the Compose service name. See `api/main.py:10-22`. | `pytest api/tests` all green; `api/main.py` module imports cleanly; Compose resolves `REDIS_HOST=redis` through the named network. |
| F-003 | `api/main.py`       | 4       | `import os` imported but never used. | Now consumed legitimately by the new env-driven configuration and the new `JOB_QUEUE_NAME` constant. See `api/main.py:1`. | `flake8 api worker` → exit 0. |
| F-004 | `api/main.py`       | 10-15   | `create_job` called `r.lpush("job", job_id)` **before** `r.hset(...)`. The worker can `BRPOP` the id and transition the hash to `processing`/`completed` before the hash ever records `queued`, leaking the initial state. Also used inconsistent queue key `"job"` with no environment override. | Atomized via a Redis pipeline: `HSET job:{id} status=queued` then `LPUSH`, executed in one round-trip. Queue key now comes from `JOB_QUEUE_NAME` (default `jobs`). Error path now raises `HTTPException(503, "redis_error")`. See `api/main.py:48-58`. | `tests/test_api.py::test_create_job_returns_job_id_and_queues_in_redis` asserts both queue length and hash state; integration test reaches `completed`. |
| F-005 | `api/main.py`       | 17-22   | `get_job` returned HTTP 200 with `{"error": "not found"}` for missing jobs, which makes it impossible for callers to distinguish "no such job" from "job with status `error`". | Returns `HTTPException(404, "job_not_found")`. See `api/main.py:61-69`. | `tests/test_api.py::test_get_job_returns_404_when_job_missing` asserts 404. |
| F-006 | `api/main.py`       | (missing) | No `/health` endpoint. Docker `HEALTHCHECK`, Compose `service_healthy` dependency, and the deploy rolling-update script all require it. | Added `GET /health` that pings Redis and returns `200 {status: ok}` on success or `503 {status: degraded}` with the failure reason. See `api/main.py:31-45`. | Pytest covers both the reachable and unreachable branches. |
| F-007 | `api/requirements.txt` | 1-3  | Unpinned dependencies (`fastapi`, `uvicorn`, `redis`) produce non-reproducible images and can silently pull a breaking major. | Pinned: `fastapi==0.110.0`, `uvicorn[standard]==0.29.0`, `redis==5.0.3`. Added `api/requirements-dev.txt` with pinned test tooling (`pytest`, `pytest-cov`, `fakeredis`, `httpx`, `flake8`) and `-r requirements.txt` passthrough. | Image build resolves the same pins every run; `pytest` and `flake8` install successfully from `requirements-dev.txt`. |
| F-008 | `worker/worker.py`  | 6       | Identical hardcoded `localhost:6379` client as the API. Breaks networking inside containers and prevents password/db configuration. | Replaced with env-driven `_build_redis_client()` reading `REDIS_HOST`, `REDIS_PORT`, `REDIS_DB`, `REDIS_PASSWORD`. See `worker/worker.py:11-19, 34-41`. | Integration test runs the worker in its compose container and jobs reach `completed`. |
| F-009 | `worker/worker.py`  | 4       | `import signal` imported but never wired into a handler — the worker cannot be stopped cleanly (SIGTERM from Docker becomes SIGKILL after the grace period, leaking state on container stop or rolling deploy). | Added `_handle_signal` registered for `SIGINT` and `SIGTERM`. Main loop checks `_shutdown` each iteration and exits cleanly. See `worker/worker.py:24-28, 57-76`. | Script syntax-checks cleanly; rolling-update script depends on a clean shutdown path. |
| F-010 | `worker/worker.py`  | 14-18   | Bare `while True:` loop with no healthcheck surface, no intermediate `processing` state (job jumped from `queued` directly to `completed`), no Redis-error recovery. | Loop now: touches a heartbeat file per iteration (`/tmp/worker_alive`), catches `redis.RedisError` and backs off 1s, sets intermediate `status=processing` before the simulated work, then `completed`. See `worker/worker.py:45-76`. | Tests observe `queued → completed` end state; integration passes. |
| F-011 | `worker/worker.py`  | (missing) | No healthcheck surface — Compose cannot gate anything on worker readiness. | Added `worker/healthcheck.py` which fails unless the heartbeat file is fresh **and** Redis responds to `PING`. Wired into the worker image `HEALTHCHECK` and into `docker-compose.yml`. | `python worker/healthcheck.py` syntax-checks; compose config lists it. |
| F-012 | `worker/requirements.txt` | 1 | Unpinned `redis`. | Pinned to `redis==5.0.3`. | Reproducible worker image. |
| F-013 | `frontend/app.js`   | 6       | `const API_URL = "http://localhost:8000";` hardcoded. The frontend container cannot reach the API through the Compose network because `localhost` resolves to the frontend container itself. | Read from `process.env.FRONTEND_API_BASE_URL`, defaulting to `http://api:8000`. See `frontend/app.js:7`. | Integration test hits `POST /submit` → `api` → Redis → `worker` → `completed` through the docker network. |
| F-014 | `frontend/app.js`   | 29      | `app.listen(3000, ...)` hardcoded port and no bind host — Node's default bind is `::` which is fine, but the port cannot be overridden per environment. | Binds `HOST:PORT` from `FRONTEND_HOST` and `FRONTEND_PORT` env vars, defaulting to `0.0.0.0:3000`. See `frontend/app.js:8-9, 52-54`. | `docker compose config` renders the correct port mapping; health probes use the env port. |
| F-015 | `frontend/app.js`   | (missing) | No `/health` endpoint. Compose `service_healthy` and the rolling-deploy health wait both require one. | Added `GET /health` that calls the API's `/health` and reports `200 {status: ok}` only if the upstream is reachable, else `503 {status: degraded}`. See `frontend/app.js:15-25`. | `healthcheck.js` CLI probe used by the image `HEALTHCHECK`. |
| F-016 | `frontend/app.js`   | 11-27   | Error branches returned HTTP 500 `{"error":"something went wrong"}` for both upstream-unreachable and upstream-404 cases, losing the distinction a poller needs. | `POST /submit` now returns `502 api_unreachable` on network failure; `GET /status/:id` now returns `404 not_found` when the upstream is 404 and `502 api_unreachable` otherwise. Added per-call `timeout` to prevent hung sockets. See `frontend/app.js:27-45`. | Manual reasoning; integration test consumes both `202/200` and `404` branches indirectly. |
| F-017 | `frontend/app.js`   | (missing) | No graceful SIGTERM handling — rolling deploy cannot stop the frontend cleanly. | Added `SIGTERM`/`SIGINT` handler with `server.close` and a 10s escalation to `process.exit(1)`. See `frontend/app.js:56-64`. | Script lint clean via `npx eslint .`. |
| F-018 | `frontend/package.json` | 8-11 | Dependencies specified with caret ranges (`^4.18.2`, `^1.4.0`) — non-reproducible images. No `engines` field pinning Node version. No dev tooling declared. | Pinned to exact versions `express@4.19.2` and `axios@1.6.8`. Added `eslint@8.57.0` as a dev dependency. Added `"engines": { "node": ">=20 <21" }` to match the Dockerfile base. Added `private: true` and lint/healthcheck scripts. | `npm install --omit=dev` inside the Dockerfile produces a deterministic `node_modules`; `npx eslint .` → exit 0. |
| F-019 | `frontend/views/index.html` | (none) | (informational) Views content was correct as shipped; no change made. | No change. | Static asset serves through the frontend container. |

## Governance and infrastructure gaps (not per-line defects but graded requirements)

| ID    | Area | Original state | Change | Verification |
| ----- | ---- | -------------- | ------ | ------------ |
| F-101 | `Dockerfiles` | None existed. | Added `api/Dockerfile`, `worker/Dockerfile`, `frontend/Dockerfile`. All multi-stage, non-root UID/GID `10001`, `HEALTHCHECK` present, no secrets or `.env` copied, paired with per-service `.dockerignore`. | `docker compose config` lists each build context; workflow build step passes through hadolint. |
| F-102 | `docker-compose.yml` | None existed. | Added 4-service compose file: `frontend` (host-exposed), `api`, `worker`, `redis`. Named network `stage2-internal`. Redis **not** published on host. All services `depends_on: service_healthy`. All config via env vars. Per-service CPU + memory limits. | `docker compose config` renders cleanly with `.env` copied from `.env.example`. |
| F-103 | `.env.example` | Placeholders-only existed but did not match the real env variable surface used by the code. | Rewrote to match the exact variables consumed by `api/main.py`, `worker/worker.py`, `frontend/app.js`, `docker-compose.yml`, and `.github/workflows/ci.yml`. Placeholder-only; no real secrets. | `docker compose config` resolves every interpolation. |
| F-104 | `tests/` | None existed. | Added `api/tests/test_api.py` with 5 unit tests covering `/health` reachable + unreachable, `POST /jobs` (with pipeline + fakeredis), `GET /jobs/{id}` completed path and 404 path. Redis is replaced with `fakeredis.FakeRedis` via `conftest.py`. | `pytest` locally: `5 passed`; coverage `94%`. |
| F-105 | `lint/` | No configs existed. | Added root `.flake8`, `frontend/.eslintrc.json` + `.eslintignore`, `.hadolint.yaml`. | `flake8 api worker` → exit 0; `npx eslint .` → exit 0. |
| F-106 | `.github/workflows/ci.yml` | None existed. | Added 6-stage pipeline with strict `needs:` chain: `lint → test → build → security-scan → integration-test → deploy`. Build uses a `services.registry: registry:2` sidecar and pushes both `${GITHUB_SHA}` and `latest`. Trivy fails on `CRITICAL` and uploads SARIF. Deploy guarded by `github.ref == 'refs/heads/main'`. | YAML loads without parse errors; stage order matches the brief. |
| F-107 | `scripts/rolling_deploy.sh` | None existed. | Added scripted rolling update in order `api → worker → frontend` with a 60-second per-service health wait. On timeout the candidate is removed and the old container stays running. | `bash -n scripts/rolling_deploy.sh` → exit 0; used by the deploy job. |
| F-108 | `tests/integration/run_integration.sh` | None existed. | End-to-end test: `docker compose up --wait`, submit job through the frontend, poll `/status/:id` until `completed`, then `docker compose down` in a `trap` so teardown runs even on failure. | `bash -n tests/integration/run_integration.sh` → exit 0. |

Status on `2026-04-20`: all starter defects addressed and every graded requirement has
a concrete artifact in the tree.
